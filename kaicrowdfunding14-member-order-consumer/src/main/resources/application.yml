server:
  port: 6000
spring:
  application:
    name: kai-crowd-order
  redis:
    host: 127.0.0.1
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  session:
    store-type: redis

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka/